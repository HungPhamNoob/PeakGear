<?php
/** @var \Vendor\CurrencyRate\Block\CurrencyRate $block */
$rates = $block->getAllRates();
?>

<section class="news-currency-section">
    <div class="news-widget-head">
        <h2>Bảng tỉ giá ngoại tệ</h2>
        <p>Nguồn: Vietcombank XML API (b=68)</p>
    </div>
    <?php if (!empty($rates)): ?>
    <div class="news-currency-table-wrap">
        <table class="news-currency-table">
            <thead>
                <tr>
                    <th>Mã</th>
                    <th>Mua CK</th>
                    <th>Mua tiền mặt</th>
                    <th>Bán</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($rates as $code => $rate): ?>
                <tr>
                    <td>
                        <span class="news-currency-code"><?= $block->escapeHtml($code) ?></span>
                        <span class="news-currency-name"><?= $block->escapeHtml((string)$rate['name']) ?></span>
                    </td>
                    <td><?= $block->escapeHtml($block->formatRate((float)$rate['buy_transfer'])) ?></td>
                    <td><?= $block->escapeHtml($block->formatRate((float)$rate['buy_cash'])) ?></td>
                    <td><?= $block->escapeHtml($block->formatRate((float)$rate['sell'])) ?></td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
    <?php else: ?>
    <p class="news-currency-empty">Không lấy được dữ liệu tỉ giá tại thời điểm hiện tại.</p>
    <?php endif; ?>
</section>
