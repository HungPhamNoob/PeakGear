<?php
/**
 * PeakGear Climbing Theme - Account Edit
 * 
 * @var \Magento\Framework\View\Element\Template $block
 */
?>

<div class="main-content">
    <section class="edit-hero">
        <div class="edit-hero-content">
            <h1>Chỉnh sửa tài khoản</h1>
        </div>
    </section>
    
    <div class="edit-container">
        <div class="edit-form-card">
            <h2 class="form-title">Thông tin cá nhân</h2>
            
            <form action="<?= $block->escapeUrl($block->getUrl('customer/account/editPost')) ?>" 
                  method="post" 
                  id="form-validate" 
                  enctype="multipart/form-data" 
                  data-mage-init='{"validation":{}}'>
                <?= $block->getBlockHtml('formkey') ?>
                
                <div class="field">
                    <label for="firstname">Họ</label>
                    <input type="text" name="firstname" id="firstname" 
                           value="<?= $block->escapeHtmlAttr($block->getCustomer()->getFirstname()) ?>"
                           data-validate="{required:true}"/>
                </div>
                
                <div class="field">
                    <label for="lastname">Tên</label>
                    <input type="text" name="lastname" id="lastname" 
                           value="<?= $block->escapeHtmlAttr($block->getCustomer()->getLastname()) ?>"
                           data-validate="{required:true}"/>
                </div>
                
                <div class="field">
                    <label for="email">Email</label>
                    <input type="email" name="email" id="email" 
                           value="<?= $block->escapeHtmlAttr($block->getCustomer()->getEmail()) ?>"
                           data-validate="{required:true, 'validate-email':true}"/>
                </div>
                
                <div class="field">
                    <label for="current-password">Mật khẩu hiện tại (để thay đổi email)</label>
                    <input type="password" name="current_password" id="current-password" 
                           class="validate-admin"/>
                </div>
                
                <div class="form-actions">
                    <button type="submit" class="btn-save">
                        Lưu thay đổi
                    </button>
                    <a href="<?= $block->escapeUrl($block->getUrl('customer/account')) ?>" class="btn-back">
                        Quay lại
                    </a>
                </div>
            </form>
        </div>
        
        <div class="edit-form-card" style="margin-top: 1.5rem;">
            <h2 class="form-title">Đổi mật khẩu</h2>
            
            <form action="<?= $block->escapeUrl($block->getUrl('customer/account/editPost')) ?>" 
                  method="post" 
                  id="change-password-form"
                  data-mage-init='{"validation":{}}'>
                <?= $block->getBlockHtml('formkey') ?>
                
                <div class="field">
                    <label for="password">Mật khẩu mới</label>
                    <input type="password" name="password" id="password" 
                           data-password-min-length="<?= $block->escapeHtmlAttr($block->getMinimumPasswordLength()) ?>"
                           data-password-min-character-sets="<?= $block->escapeHtmlAttr($block->getRequiredCharacterClassesNumber()) ?>"
                           data-validate="{required:true, 'validate-customer-password':true}"/>
                </div>
                
                <div class="field">
                    <label for="password_confirmation">Xác nhận mật khẩu mới</label>
                    <input type="password" name="password_confirmation" id="password_confirmation" 
                           data-validate="{required:true, equalTo:'#password'}"/>
                </div>
                
                <div class="form-actions">
                    <button type="submit" class="btn-save">
                        Đổi mật khẩu
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
