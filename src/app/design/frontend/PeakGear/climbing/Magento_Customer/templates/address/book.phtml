<?php
/**
 * PeakGear Climbing Theme - Address Book Front Page
 *
 * @var \Magento\Customer\Block\Address\Book $block
 * @var \Magento\Framework\Escaper $escaper
 */
?>
<div class="main-content">
    <section class="edit-hero">
        <div class="edit-hero-content">
            <h1><?= $escaper->escapeHtml(__('Sổ Địa Chỉ')) ?></h1>
            <p style="color: fade(#fff, 50%); margin-top: 0.5rem;">Cấu hình mặc định dành cho giao nhận và thanh toán</p>
        </div>
    </section>

    <div class="edit-container">
        <!-- Default Addresses -->
        <h2 class="form-title" style="margin-bottom: 1.5rem;">
            <?= $escaper->escapeHtml(__('Địa chỉ mặc định')) ?>
        </h2>

        <div class="field-row">
            <?php if ($_pAddsses = $block->getDefaultBilling()) : ?>
                <div class="edit-form-card" style="margin-bottom: 0;">
                    <h3 class="account-card-title form-title" style="font-size: 1rem; padding-bottom: 0.5rem; border-color: rgba(90, 138, 60, 0.3); color: #c8ddb8;">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 1.25rem; height: 1.25rem; margin-right: 0.5rem; color: #5a8a3c;">
                            <rect x="2" y="5" width="20" height="14" rx="2"/>
                            <line x1="2" y1="10" x2="22" y2="10"/>
                        </svg>
                        <?= $escaper->escapeHtml(__('Địa chỉ thanh toán mặc định')) ?>
                    </h3>
                    <div class="account-card-content" style="margin: 1.5rem 0; line-height: 1.8;">
                        <address>
                            <?= $block->getAddressHtml($block->getAddressById($_pAddsses)) ?>
                        </address>
                    </div>
                    <div class="form-actions" style="border-top: 1px solid rgba(255,255,255,0.1); padding-top: 1.5rem; justify-content: flex-start;">
                        <a class="btn-save" href="<?= $escaper->escapeUrl($block->getAddressEditUrl($_pAddsses)) ?>">
                            <?= $escaper->escapeHtml(__('Chỉnh sửa')) ?>
                        </a>
                    </div>
                </div>
            <?php else : ?>
                <div class="edit-form-card" style="margin-bottom: 0;">
                    <h3 class="account-card-title form-title" style="font-size: 1rem; padding-bottom: 0.5rem;">
                        <span><?= $escaper->escapeHtml(__('Địa chỉ thanh toán mặc định')) ?></span>
                    </h3>
                    <div class="account-card-content" style="margin: 1.5rem 0; color: rgba(255,255,255,0.5);">
                        <p><?= $escaper->escapeHtml(__('Bạn vẫn chưa thêm địa chỉ thanh toán mặc định.')) ?></p>
                    </div>
                </div>
            <?php endif ?>

            <?php if ($_pAddsses = $block->getDefaultShipping()) : ?>
                <div class="edit-form-card" style="margin-bottom: 0;">
                    <h3 class="account-card-title form-title" style="font-size: 1rem; padding-bottom: 0.5rem; border-color: rgba(255, 107, 107, 0.3); color: #ffb8b8;">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 1.25rem; height: 1.25rem; margin-right: 0.5rem; color: #ff6b6b;">
                            <path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"/>
                            <circle cx="12" cy="10" r="3"/>
                        </svg>
                        <?= $escaper->escapeHtml(__('Địa chỉ giao hàng mặc định')) ?>
                    </h3>
                    <div class="account-card-content" style="margin: 1.5rem 0; line-height: 1.8;">
                        <address>
                            <?= $block->getAddressHtml($block->getAddressById($_pAddsses)) ?>
                        </address>
                    </div>
                    <div class="form-actions" style="border-top: 1px solid rgba(255,255,255,0.1); padding-top: 1.5rem; justify-content: flex-start;">
                        <a class="btn-save" href="<?= $escaper->escapeUrl($block->getAddressEditUrl($_pAddsses)) ?>">
                            <?= $escaper->escapeHtml(__('Chỉnh sửa')) ?>
                        </a>
                    </div>
                </div>
            <?php else : ?>
                <div class="edit-form-card" style="margin-bottom: 0;">
                    <h3 class="account-card-title form-title" style="font-size: 1rem; padding-bottom: 0.5rem;">
                        <span><?= $escaper->escapeHtml(__('Địa chỉ giao hàng mặc định')) ?></span>
                    </h3>
                    <div class="account-card-content" style="margin: 1.5rem 0; color: rgba(255,255,255,0.5);">
                        <p><?= $escaper->escapeHtml(__('Bạn vẫn chưa thiết lập giao hàng mặc định.')) ?></p>
                    </div>
                </div>
            <?php endif ?>
        </div>
        
        <!-- Break UI visually before grid -->
        <div style="margin: 3rem 0 1.5rem; display: flex; align-items: center; justify-content: space-between;">
            <h2 class="form-title" style="margin: 0; border: none; padding: 0;">
                <?= $escaper->escapeHtml(__('Địa chỉ khác')) ?>
            </h2>
            <a class="btn-save" href="<?= $escaper->escapeUrl($block->getAddAddressUrl()) ?>" style="width: auto; padding: 0.5rem 1rem; border-radius: 2rem; display: flex; align-items: center; gap: 0.5rem;">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 1rem; height: 1rem;">
                    <line x1="12" y1="5" x2="12" y2="19"/>
                    <line x1="5" y1="12" x2="19" y2="12"/>
                </svg>
                Thêm địa chỉ mới
            </a>
        </div>

        <?= $block->getChildHtml('additional_addresses') ?>
        
        <div class="form-actions" style="margin-top: 2rem;">
            <a class="btn-back" href="<?= $escaper->escapeUrl($block->getBackUrl()) ?>">
                <?= $escaper->escapeHtml(__('Quay lại bảng điều khiển')) ?>
            </a>
        </div>
    </div>
</div>
