<?php
/**
 * PeakGear Climbing Theme - Additional Addresses Grid
 *
 * @var \Magento\Customer\Block\Address\Grid $block
 * @var \Magento\Framework\Escaper $escaper
 */
?>

<div class="block block-addresses-list" style="margin-bottom: 3rem;">
    <div class="block-content">
        <?php if ($_pAddsses = $block->getAdditionalAddresses()) : ?>

            <div class="field-row">
                <?php foreach ($_pAddsses as $address) : ?>
                    <div class="edit-form-card" style="margin-bottom: 0;">
                        <div class="account-card-content" style="margin: 0; line-height: 1.8; color: fade(#fff, 85%);">
                            <address style="margin-bottom: 1rem;">
                                <strong><?= $escaper->escapeHtml($address->getFirstname() . ' ' . $address->getLastname()) ?></strong><br>
                                <?= $escaper->escapeHtml($block->getStreetAddress($address)) ?><br>
                                <?= $escaper->escapeHtml($address->getCity()) ?>, <?= $escaper->escapeHtml($address->getRegion()->getRegion()) ?><br>
                                <?= $escaper->escapeHtml($block->getCountryByCode($address->getCountryId())) ?><br>
                                <span style="display:inline-block; margin-top: 0.5rem; color: fade(#fff, 60%);">
                                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 1rem; height: 1rem; vertical-align: middle; margin-right: 0.25rem;">
                                        <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                                    </svg>
                                    <?= $escaper->escapeHtml($address->getTelephone()) ?>
                                </span>
                            </address>
                        </div>
                        <div class="form-actions" style="border-top: 1px solid rgba(255,255,255,0.1); padding-top: 1.25rem; margin-top: auto;">
                            <!-- Edit Button -->
                            <a class="btn-save" href="<?= $escaper->escapeUrl($block->getUrl('customer/address/edit', ['id' => $address->getId()])) ?>" style="flex: 1; text-align: center; padding: 0.5rem; border-radius: 0.5rem;">
                                Sửa
                            </a>
                            <!-- Delete Button -->
                            <a class="action delete" href="#" role="delete-address" data-address="<?= $escaper->escapeHtmlAttr($address->getId()) ?>" style="flex: 1; text-align: center; padding: 0.5rem; border-radius: 0.5rem; background: rgba(255,107,107,0.1); color: #ff6b6b; text-decoration: none; font-weight: 500;">
                                Xóa
                            </a>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>

            <?php if ($block->getChildHtml('pager')) : ?>
                <div class="customer-addresses-toolbar toolbar bottom" style="margin-top: 2rem;"><?= $block->getChildHtml('pager') ?></div>
            <?php endif ?>
        <?php else : ?>
            <div class="edit-form-card">
                <p style="color: fade(#fff, 50%); margin: 0; text-align: center; padding: 2rem 0;">
                    <?= $escaper->escapeHtml(__('Bạn chưa có địa chỉ phụ nào.')) ?>
                </p>
            </div>
        <?php endif ?>
    </div>
</div>

<script type="text/x-magento-init">
    {
        ".page-main": {
            "address": {
                "deleteAddress": "a[role='delete-address']",
                "deleteUrlPrefix": "<?= $escaper->escapeJs($escaper->escapeUrl($block->getDeleteUrl())) ?>id/",
                "addAddress": "button[role='add-address']",
                "addAddressLocation": "<?= $escaper->escapeJs($escaper->escapeUrl($block->getAddAddressUrl())) ?>"
            }
        }
    }
</script>
