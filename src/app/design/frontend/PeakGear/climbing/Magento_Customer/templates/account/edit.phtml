<?php
/**
 * PeakGear Climbing Theme - Account Info (Edit + Address)
 *
 * @var \Magento\Customer\Block\Account\Edit $block
 */

// Get customer from session using ObjectManager
$objectManager = \Magento\Framework\App\ObjectManager::getInstance();
$customerSession = $objectManager->get(\Magento\Customer\Model\Session::class);
$customer = $customerSession->getCustomer();

// Get customer data with null checks
$firstname = ($customer && $customer->getId()) ? $customer->getFirstname() : '';
$lastname = ($customer && $customer->getId()) ? $customer->getLastname() : '';
$email = ($customer && $customer->getId()) ? $customer->getEmail() : '';
?>

<div class="main-content">
    <section class="edit-hero">
        <div class="edit-hero-content">
            <h1>Thông Tin Cá Nhân</h1>
        </div>
    </section>

    <div class="edit-container">
        <!-- Personal Info Form -->
        <div class="edit-form-card">
            <h2 class="form-title">Thông tin tài khoản</h2>

            <form action="<?= $block->escapeUrl($block->getUrl('customer/account/editPost')) ?>"
                  method="post"
                  id="form-validate"
                  enctype="multipart/form-data"
                  data-mage-init='{"validation":{}}'>
                <?= $block->getBlockHtml('formkey') ?>

                <div class="field-row">
                    <div class="field">
                        <label for="firstname">Họ</label>
                        <input type="text" name="firstname" id="firstname"
                               value="<?= $block->escapeHtmlAttr($firstname) ?>"
                               placeholder="Nhập họ của bạn"
                               data-validate="{required:true}"/>
                    </div>

                    <div class="field">
                        <label for="lastname">Tên</label>
                        <input type="text" name="lastname" id="lastname"
                               value="<?= $block->escapeHtmlAttr($lastname) ?>"
                               placeholder="Nhập tên của bạn"
                               data-validate="{required:true}"/>
                    </div>
                </div>

                <div class="field">
                    <label for="email">Địa chỉ Email</label>
                    <input type="email" name="email" id="email"
                           value="<?= $block->escapeHtmlAttr($email) ?>"
                           placeholder="example@email.com"
                           data-validate="{required:true, 'validate-email':true}"/>
                </div>

                <div class="field">
                    <label for="current-password">Mật khẩu hiện tại <span style="color: rgba(255,255,255,0.5); font-weight: normal;">(bắt buộc khi đổi email)</span></label>
                    <input type="password" name="current_password" id="current-password"
                           placeholder="Nhập mật khẩu hiện tại"/>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn-save">
                        Lưu thay đổi
                    </button>
                    <a href="<?= $block->escapeUrl($block->getUrl('myaccount')) ?>" class="btn-back">
                        Quay lại
                    </a>
                </div>
            </form>
        </div>

        <!-- Change Password Form -->
        <div class="edit-form-card">
            <h2 class="form-title">Đổi mật khẩu</h2>

            <form action="<?= $block->escapeUrl($block->getUrl('customer/account/editPost')) ?>"
                  method="post"
                  id="change-password-form"
                  data-mage-init='{"validation":{}}'>
                <?= $block->getBlockHtml('formkey') ?>
                <input type="hidden" name="change_password" value="1"/>

                <div class="field">
                    <label for="current-password-change">Mật khẩu hiện tại</label>
                    <input type="password" name="current_password" id="current-password-change"
                           placeholder="Nhập mật khẩu hiện tại"
                           data-validate="{required:true}"/>
                </div>

                <div class="field-row">
                    <div class="field">
                        <label for="password">Mật khẩu mới</label>
                        <input type="password" name="password" id="password"
                               placeholder="Tối thiểu <?= $block->escapeHtmlAttr($block->getMinimumPasswordLength()) ?> ký tự"
                               data-password-min-length="<?= $block->escapeHtmlAttr($block->getMinimumPasswordLength()) ?>"
                               data-password-min-character-sets="<?= $block->escapeHtmlAttr($block->getRequiredCharacterClassesNumber()) ?>"
                               data-validate="{required:true, 'validate-customer-password':true}"/>
                    </div>

                    <div class="field">
                        <label for="password_confirmation">Xác nhận mật khẩu</label>
                        <input type="password" name="password_confirmation" id="password_confirmation"
                               placeholder="Nhập lại mật khẩu mới"
                               data-validate="{required:true, equalTo:'#password'}"/>
                    </div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn-save">
                        Đổi mật khẩu
                    </button>
                </div>
            </form>
        </div>

        <!-- Address Section -->
        <div class="edit-form-card">
            <h2 class="form-title">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 1.25rem; height: 1.25rem; margin-right: 0.5rem; color: #ff6b6b;">
                    <path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"/>
                    <circle cx="12" cy="10" r="3"/>
                </svg>
                Sổ địa chỉ
            </h2>

            <p style="color: rgba(255,255,255,0.6); font-size: 0.9rem; margin-bottom: 1.5rem;">
                Quản lý địa chỉ giao hàng và thanh toán của bạn.
            </p>

            <div class="form-actions">
                <a href="<?= $block->escapeUrl($block->getUrl('customer/address')) ?>" class="btn-save">
                    Quản lý địa chỉ
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 1rem; height: 1rem;">
                        <path d="M5 12h14M12 5l7 7-7 7"/>
                    </svg>
                </a>
            </div>
        </div>
    </div>
</div>
